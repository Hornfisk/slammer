# slammer (redirect)

Redirect stub for `hornfisk.github.io/slammer/` → `hyperfocusdsp.github.io/slammer/`.

The Slammer repo was transferred from `Hornfisk/slammer` to `hyperfocusdsp/slammer` on 2026-04-19. GitHub redirects the repo URL, but GitHub Pages does not — this repo exists only to serve the Pages redirect page so stale links to the old audio preview URL don't 404.

Source is at [hyperfocusdsp/slammer](https://github.com/hyperfocusdsp/slammer).
